import { PagesActiveDirective } from './pages-active.directive';

describe('PagesActiveDirective', () => {
  it('should create an instance', () => {
    const directive = new PagesActiveDirective();
    expect(directive).toBeTruthy();
  });
});
